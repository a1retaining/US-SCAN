const { round, percentMove } = require("../data/marketData");
function sma(values,period){if(!Array.isArray(values)||values.length<period)return null;const slice=values.slice(-period).filter(Number.isFinite);if(slice.length<period)return null;return slice.reduce((s,v)=>s+v,0)/slice.length;}
function ema(values,period){if(!Array.isArray(values)||values.length<period)return null;const clean=values.filter(Number.isFinite);if(clean.length<period)return null;const k=2/(period+1);let cur=clean.slice(0,period).reduce((s,v)=>s+v,0)/period;for(let i=period;i<clean.length;i++)cur=clean[i]*k+cur*(1-k);return cur;}
function rsi(values,period=14){if(!Array.isArray(values)||values.length<=period)return null;let gains=0,losses=0;for(let i=values.length-period;i<values.length;i++){const ch=values[i]-values[i-1];if(ch>=0)gains+=ch;else losses-=ch;}if(losses===0)return 100;const rs=gains/losses;return 100-(100/(1+rs));}
function atr(bars,period=14){if(!Array.isArray(bars)||bars.length<=period)return null;const trs=[];for(let i=1;i<bars.length;i++){const h=bars[i].high,l=bars[i].low,pc=bars[i-1].close;trs.push(Math.max(h-l,Math.abs(h-pc),Math.abs(l-pc)));}return sma(trs,period);}
function marketRegimeFromSignals(signals,vix){const breadth=signals.length?Math.round((signals.filter(s=>s.trend==="UP").length/signals.length)*100):0;const volatility=Number.isFinite(vix)?(vix<18?"LOW":vix<25?"MEDIUM":"HIGH"):"UNKNOWN";let regime="NEUTRAL";if(breadth>=60&&volatility!=="HIGH")regime="BULLISH";if(breadth<45||volatility==="HIGH")regime="BEARISH";return{regime,breadth,volatility};}
function historicalScore(edge,setupType,settings){if(!edge||!edge.summary)return{score:0,grade:"NO EDGE DATA",passed:!settings.requireHistoricalEdge,warning:"No 3-year historical edge data yet."};const st=edge.setupStats?.[setupType]||edge.summary,trades=Number(st.trades||0),expectancyR=Number(st.expectancyR||0),profitFactor=Number(st.profitFactor||0),winRate=Number(st.winRate||0);let score=0;if(trades>=settings.minHistoricalTrades)score+=25;if(expectancyR>=settings.minHistoricalExpectancyR)score+=30;if(profitFactor>=settings.minHistoricalProfitFactor)score+=25;if(winRate>=55)score+=10;if((st.maxDrawdownR||999)<=8)score+=10;const passed=trades>=settings.minHistoricalTrades&&expectancyR>=settings.minHistoricalExpectancyR&&profitFactor>=settings.minHistoricalProfitFactor;return{score:Math.max(0,Math.min(100,Math.round(score))),grade:score>=80?"STRONG EDGE":score>=60?"FAIR EDGE":"WEAK EDGE",passed,warning:passed?"":"Historical edge is weak or sample is too small.",stats:st};}
function buildSignal(symbol,bars,spyMove21,marketBias,settings={},historicalEdges={}){const closes=bars.map(b=>b.close),volumes=bars.map(b=>b.volume||0),last=bars[bars.length-1],prev=bars[bars.length-2]||last,price=last.close,ema20=ema(closes,20),ema50=ema(closes,50),sma200=sma(closes,Math.min(200,closes.length)),rsi14=rsi(closes,14),atr14=atr(bars,14),avgVol=sma(volumes,20),volumeRatio=avgVol?last.volume/avgVol:null,move1=percentMove(price,prev.close),move21=closes.length>22?percentMove(closes[closes.length-1],closes[closes.length-22]):0,relativeStrength=move21-spyMove21,recent20=bars.slice(-20),high20=Math.max(...recent20.map(b=>b.high)),low20=Math.min(...recent20.map(b=>b.low)),trendBull=price>ema20&&price>ema50&&price>sma200,setup=price>=high20*0.985?"Breakout":price>ema20?"Pullback":"Watch";let technicalScore=0;const reasons=[],warnings=[];if(price>ema20){technicalScore+=15;reasons.push("Price is above EMA20.");}else warnings.push("Price is below EMA20.");if(price>ema50){technicalScore+=15;reasons.push("Price is above EMA50.");}else warnings.push("Price is below EMA50.");if(price>sma200){technicalScore+=14;reasons.push("Price is above long-term trend.");}else warnings.push("Price is below long-term trend.");if(ema20>ema50){technicalScore+=12;reasons.push("EMA20 is above EMA50.");}else warnings.push("EMA20 is not above EMA50.");if(relativeStrength>0){technicalScore+=12;reasons.push("Relative strength is stronger than SPY.");}else warnings.push("Relative strength is weaker than SPY.");if(rsi14>=50&&rsi14<=70){technicalScore+=10;reasons.push("RSI is in a healthy bullish zone.");}else if(rsi14>72)warnings.push("RSI is extended.");else warnings.push("RSI is not bullish enough.");if(volumeRatio&&volumeRatio>=1.15){technicalScore+=8;reasons.push("Volume is above average.");}if(setup==="Breakout"){technicalScore+=8;reasons.push("Price is near a 20-day breakout area.");}if(marketBias==="BULLISH")technicalScore+=6;if(marketBias==="BEARISH")technicalScore-=12;const stretched=(rsi14&&rsi14>74)||(ema20&&price>ema20*1.08);if(stretched){technicalScore-=12;warnings.push("Price may be extended; avoid chasing.");}technicalScore=Math.max(1,Math.min(99,Math.round(technicalScore)));const edge=historicalScore(historicalEdges[symbol],setup,settings);if(edge.warning)warnings.push(edge.warning);if(edge.passed)reasons.push("3-year historical edge filter passed.");const safeAtr=Number.isFinite(atr14)&&atr14>0?atr14:price*0.035,stop=round(Math.max(0.01,Math.min(low20*0.985,price-safeAtr*1.45))),risk=Math.max(0.01,price-stop),target1=round(price+risk*1.8),target2=round(price+risk*2.6),rrNumber=round((target1-price)/risk,2);if(rrNumber<settings.minRiskReward)warnings.push(`Risk/reward is below ${settings.minRiskReward}.`);if(marketBias!=="BULLISH")warnings.push("Market regime is not strongly bullish.");if(!trendBull)warnings.push("Trend filter is not fully bullish.");const confidence=Math.max(1,Math.min(99,Math.round((technicalScore*0.65)+(edge.score*0.35))));let safety="REJECT";if(confidence>=settings.minConfidence&&trendBull&&marketBias==="BULLISH"&&rrNumber>=settings.minRiskReward&&!stretched&&edge.passed)safety="TRADE_READY";else if(confidence>=68)safety="WATCHLIST";return{symbol,price:round(price),changePct:round(move1,2),setup,confidence,technicalScore,historicalScore:edge.score,historicalGrade:edge.grade,historicalStats:edge.stats||null,rr:`${rrNumber}:1`,rrNumber,trend:trendBull?"UP":"NEUTRAL",regime:trendBull?"Bullish":"Neutral",action:safety==="TRADE_READY"?"LONG":safety==="WATCHLIST"?"WATCH":"IGNORE",safety,entry:round(price),buyLow:round(price*0.992),buyHigh:round(price*1.006),stop,target1,target2,rsi:round(rsi14),relativeStrength:round(relativeStrength,2),volumeRatio:round(volumeRatio,2),reasons,warnings,bars:bars.slice(-180)};}
function scanMarket(barsBySymbol,settings={},historicalEdges={}){const spyBars=barsBySymbol.SPY||[],spyCloses=spyBars.map(b=>b.close),spyMove21=spyCloses.length>22?percentMove(spyCloses[spyCloses.length-1],spyCloses[spyCloses.length-22]):0,excluded=new Set(["DIA","IWM","VIX"]);const preliminary=Object.keys(barsBySymbol).filter(s=>!excluded.has(s)).map(s=>buildSignal(s,barsBySymbol[s],spyMove21,"NEUTRAL",settings,historicalEdges));const vix=barsBySymbol.VIX?.at(-1)?.close,preRegime=marketRegimeFromSignals(preliminary,vix);const signals=Object.keys(barsBySymbol).filter(s=>!excluded.has(s)).map(s=>buildSignal(s,barsBySymbol[s],spyMove21,preRegime.regime,settings,historicalEdges)).sort((a,b)=>b.confidence-a.confidence).map((s,i)=>({rank:i+1,...s}));const finalRegime=marketRegimeFromSignals(signals,vix),spySignal=signals.find(s=>s.symbol==="SPY"),qqqSignal=signals.find(s=>s.symbol==="QQQ");const market={regime:finalRegime.regime,spyTrend:spySignal?.trend==="UP"?"BULLISH":"NEUTRAL",qqqTrend:qqqSignal?.trend==="UP"?"BULLISH":"NEUTRAL",volatility:finalRegime.volatility,vix:round(vix),breadth:`${finalRegime.breadth}%`,breadthScore:finalRegime.breadth,sectorLeader:"TECHNOLOGY",confidence:Math.round((finalRegime.breadth+(spySignal?.confidence||50))/2)};return{market,signals};}
module.exports={scanMarket,buildSignal,marketRegimeFromSignals,sma,ema,rsi,atr,historicalScore};