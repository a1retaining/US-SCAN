# TradingMint PRO

Fixed version. Sidebar navigation is clickable and dashboard matches the requested TradingMint PRO terminal direction.

Run:

```bash
npm install
npm test
npm start
```

Render build command: `npm install`

Render start command: `node src/server.js`
